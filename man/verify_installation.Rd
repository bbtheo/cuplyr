% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify.R
\name{verify_installation}
\alias{verify_installation}
\title{Verify cuplyr installation}
\usage{
verify_installation()
}
\value{
Invisibly returns a list with:
\describe{
\item{ok}{Logical: \code{TRUE} if all checks pass}
\item{package_loaded}{Logical: package loads without error}
\item{gpu_available}{Logical: GPU detected and accessible}
\item{gpu_name}{Character: GPU model name, or \code{NA}}
\item{roundtrip_ok}{Logical: data round-trip succeeded}
\item{time_seconds}{Numeric: time for the round-trip test}
}
}
\description{
Performs a post-install runtime verification by running a small GPU
round-trip: creates a GPU table, performs a filter, and collects back
to R. This confirms that the package loads, GPU is accessible, and
basic operations work.
}
\details{
This function runs three checks in sequence:
\enumerate{
\item \strong{Package load}: \code{library(cuplyr)} succeeds
\item \strong{GPU detection}: \code{gpu_details()} shows an available GPU
\item \strong{Round-trip}: \code{tbl_gpu(mtcars) |> filter(mpg > 20) |> collect()}
returns correct results
}
}
\examples{
\dontrun{
result <- verify_installation()
if (result$ok) {
  message("cuplyr is working!")
}
}
}
\seealso{
\code{\link[=check_deps]{check_deps()}} for pre-install dependency checks,
\code{\link[=diagnostics]{diagnostics()}} for full system diagnostics
}
