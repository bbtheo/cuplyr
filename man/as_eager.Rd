% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute.R
\name{as_eager}
\alias{as_eager}
\title{Switch to eager execution mode}
\usage{
as_eager(.data)
}
\arguments{
\item{.data}{A \code{tbl_gpu} object.}
}
\value{
A \code{tbl_gpu} in eager execution mode.
}
\description{
Computes any pending operations and returns a \code{tbl_gpu} that will
execute all subsequent operations immediately.
}
\examples{
if (has_gpu()) {
  # Start lazy, switch to eager mid-pipeline
  result <- tbl_gpu(mtcars, lazy = TRUE) |>
    filter(mpg > 20) |>
    as_eager() |>
    mutate(x = hp * 2) |>  # executes immediately
    collect()
}
}
