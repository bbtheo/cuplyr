% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-deps.R
\name{check_deps}
\alias{check_deps}
\title{Check cuplyr dependencies}
\usage{
check_deps(format = c("text", "json"), verbose = FALSE)
}
\arguments{
\item{format}{Output format: \code{"text"} for human-readable console output,
\code{"json"} for machine-readable JSON string.}

\item{verbose}{If \code{TRUE}, print additional details about each check.}
}
\value{
Invisibly returns a list with:
\describe{
\item{ok}{Logical: \code{TRUE} if all required checks pass}
\item{checks}{Named list of individual check results, each with
\code{ok}, \code{value}, and \code{message} fields}
}
}
\description{
Performs a pre-flight check of all dependencies required to install and
run cuplyr. Reports status of NVIDIA driver, CUDA toolkit, libcudf,
R version, and required R packages.
}
\details{
The following checks are performed:
\enumerate{
\item \strong{NVIDIA Driver}: Verifies \code{nvidia-smi} is available and reports
driver version
\item \strong{CUDA Toolkit}: Checks for \code{nvcc} and reports CUDA version
\item \strong{libcudf}: Looks for cudf headers in standard paths
\item \strong{R Version}: Checks R >= 4.3.0
\item \strong{R Packages}: Verifies required R packages are installed
\item \strong{GPU Access}: Tests if a GPU device is accessible (if cuplyr
is already installed)
}
}
\examples{
# Human-readable output
check_deps()

# Machine-readable JSON
check_deps(format = "json")

# Programmatic use
result <- check_deps()
if (!result$ok) {
  message("Some checks failed. See result$checks for details.")
}
}
\seealso{
\code{\link[=verify_installation]{verify_installation()}} for post-install verification,
\code{\link[=diagnostics]{diagnostics()}} for full system diagnostics
}
