% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group-by.R
\name{ungroup.tbl_gpu}
\alias{ungroup.tbl_gpu}
\title{Remove grouping from a GPU table}
\usage{
\method{ungroup}{tbl_gpu}(x, ...)
}
\arguments{
\item{x}{A \code{tbl_gpu} object.}

\item{...}{Ignored. Included for compatibility with dplyr generic.}
}
\value{
An ungrouped \code{tbl_gpu} object.
}
\description{
Removes all grouping information from a grouped \code{tbl_gpu} object.
}
\examples{
if (has_gpu()) {
  gpu_mtcars <- tbl_gpu(mtcars)

  grouped <- gpu_mtcars |>
    group_by(cyl)

  # Remove grouping
  ungrouped <- grouped |>
    ungroup()

  # Verify groups are removed
  length(group_vars(ungrouped))  # 0
}
}
