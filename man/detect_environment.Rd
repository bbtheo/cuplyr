% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{detect_environment}
\alias{detect_environment}
\title{Detect the runtime environment}
\usage{
detect_environment(override = Sys.getenv("CUPLYR_ENV"))
}
\arguments{
\item{override}{Explicit override, e.g. from \code{CUPLYR_ENV} env var.}
}
\value{
One of \code{"local"}, \code{"colab"}, \code{"container"}, \code{"cloud_gpu"}.
}
\description{
Identifies whether we're running on Colab, in a container, on a cloud GPU
instance, or locally. Used by \code{\link[=install_cuplyr]{install_cuplyr()}} to apply environment-specific
fixes (e.g. CUDA stub disabling on Colab).
}
