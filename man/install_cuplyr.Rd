% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install_cuplyr}
\alias{install_cuplyr}
\title{Install cuplyr from source}
\usage{
install_cuplyr(
  method = c("auto", "pixi", "conda", "system"),
  repo = NULL,
  ref = "master",
  conda_prefix = NULL,
  configure_args = character(),
  dry_run = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{method}{How to obtain build dependencies:
\describe{
\item{\code{"auto"}}{Try methods in order: pixi, conda, system}
\item{\code{"pixi"}}{Use pixi to manage a reproducible environment}
\item{\code{"conda"}}{Use conda/mamba to install RAPIDS into a prefix}
\item{\code{"system"}}{Assume CUDA and cuDF are already installed on the system}
}}

\item{repo}{GitHub repository URL or local path to cuplyr source.
If \code{NULL}, assumes current directory is the cuplyr source tree.}

\item{ref}{Git branch or tag to install. Only used when \code{repo} is a URL.}

\item{conda_prefix}{Path for the conda environment when \code{method = "conda"}.
Defaults to \code{"/opt/rapids"} or a temp directory.}

\item{configure_args}{Extra arguments passed to \code{./configure}.}

\item{dry_run}{If \code{TRUE}, print what would be done without executing.}

\item{verbose}{If \code{TRUE}, print detailed progress.}
}
\value{
Invisibly returns \code{TRUE} on success.
}
\description{
Installs cuplyr by setting up build dependencies (if needed) and
running \verb{./configure && R CMD INSTALL .}. Supports multiple methods
for providing CUDA/cuDF dependencies.
}
\details{
\subsection{Method details}{

\strong{auto} (default): Tries methods in order of preference:
\enumerate{
\item If \code{pixi} is on PATH and \code{pixi.toml} exists, use pixi
\item If \code{mamba} or \code{conda} is on PATH, use conda
\item If CUDA and cuDF are already available, use system
}

\strong{pixi}: Runs \verb{pixi run install} which handles everything.
Requires \href{https://pixi.sh}{pixi} to be installed.

\strong{conda}: Creates a conda environment with RAPIDS packages, then
configures and builds cuplyr against it. Requires \code{mamba} or \code{conda}.

\strong{system}: Assumes \code{CUDA_HOME} and \code{CONDA_PREFIX}/\code{CUDF_HOME} are
already set. Runs \verb{./configure && R CMD INSTALL .} directly.
}

\subsection{Pre-flight}{

Before building, \code{install_cuplyr()} runs \code{\link[=check_deps]{check_deps()}} and reports
any missing dependencies. With \code{method = "conda"}, missing RAPIDS
packages are installed automatically.
}
}
\examples{
\dontrun{
# Auto-detect best method
install_cuplyr()

# Use conda explicitly
install_cuplyr(method = "conda")

# System install (CUDA/cuDF already available)
install_cuplyr(method = "system")

# Preview what would happen
install_cuplyr(dry_run = TRUE)

# Install from GitHub
install_cuplyr(repo = "https://github.com/bbtheo/cuplyr.git")
}
}
\seealso{
\code{\link[=check_deps]{check_deps()}} for dependency checking,
\code{\link[=verify_installation]{verify_installation()}} for post-install verification,
\code{\link[=diagnostics]{diagnostics()}} for troubleshooting
}
