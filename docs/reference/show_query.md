# Show pending operations for a lazy tbl_gpu

Show pending operations for a lazy tbl_gpu

## Usage

``` r
show_query(.data)
```

## Arguments

- .data:

  A `tbl_gpu` object.

## Value

Invisibly returns the AST, prints it for inspection.
