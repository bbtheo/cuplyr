# GPU bind_rows implementation

GPU bind_rows implementation

## Usage

``` r
bind_rows_gpu(dots, .id = NULL)
```

## Arguments

- dots:

  List of tables to bind

- .id:

  Optional column name to identify source

## Value

A tbl_gpu
