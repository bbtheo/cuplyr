# Resolve execution mode from explicit value, option, or environment variable

Resolve execution mode from explicit value, option, or environment
variable

## Usage

``` r
resolve_exec_mode(explicit = NULL)
```

## Arguments

- explicit:

  Explicit value passed to tbl_gpu() or NULL

## Value

"lazy" or "eager"
