# Create a select AST node

Create a select AST node

## Usage

``` r
ast_select(input, columns)
```

## Arguments

- input:

  Input AST node

- columns:

  Character vector of column names to keep

## Value

An ast_select node
