# Add null columns to a table

Add null columns to a table

## Usage

``` r
add_null_columns(tbl, col_names, col_types)
```

## Arguments

- tbl:

  A tbl_gpu object

- col_names:

  Names of columns to add

- col_types:

  Types of columns to add

## Value

A tbl_gpu with additional null columns
