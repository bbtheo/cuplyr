# Check if a tbl_gpu has pending lazy operations

Check if a tbl_gpu has pending lazy operations

## Usage

``` r
has_pending_ops(.data)
```

## Arguments

- .data:

  A `tbl_gpu` object.

## Value

Logical. `TRUE` if there are pending operations.
