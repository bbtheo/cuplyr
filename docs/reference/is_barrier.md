# Check if an AST node is an optimization barrier

Check if an AST node is an optimization barrier

## Usage

``` r
is_barrier(node)
```

## Arguments

- node:

  An AST node

## Value

TRUE if node is a barrier
