# Reorder filters by estimated cost (cheapest first)

Reorder filters by estimated cost (cheapest first)

## Usage

``` r
reorder_filters(ast)
```

## Arguments

- ast:

  Root AST node

## Value

AST with reordered filters
