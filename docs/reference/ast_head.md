# Create a head/limit AST node

Create a head/limit AST node

## Usage

``` r
ast_head(input, n)
```

## Arguments

- input:

  Input AST node

- n:

  Number of rows to keep

## Value

An ast_head node
