# Create a group_by AST node (metadata only)

Create a group_by AST node (metadata only)

## Usage

``` r
ast_group_by(input, groups)
```

## Arguments

- input:

  Input AST node

- groups:

  Character vector of grouping columns

## Value

An ast_group_by node
