# Get column names of a GPU table

Get column names of a GPU table

## Usage

``` r
# S3 method for class 'tbl_gpu'
names(x)
```

## Arguments

- x:

  A `tbl_gpu` object.

## Value

A character vector of column names.
