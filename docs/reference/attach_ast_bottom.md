# Attach a node at the bottom of an AST

Returns a new AST with the bottom input replaced by `new_input`.

## Usage

``` r
attach_ast_bottom(ast, new_input)
```

## Arguments

- ast:

  AST node

- new_input:

  Node to attach at bottom

## Value

AST with bottom input replaced
