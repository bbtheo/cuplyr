# Cast a column to a different type

Cast a column to a different type

## Usage

``` r
cast_column(tbl, col_name, target_type)
```

## Arguments

- tbl:

  A tbl_gpu object

- col_name:

  Name of column to cast

- target_type:

  Target type string

## Value

A tbl_gpu with the column cast to the target type
