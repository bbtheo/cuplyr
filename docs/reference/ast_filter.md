# Create a filter AST node

Create a filter AST node

## Usage

``` r
ast_filter(input, predicates)
```

## Arguments

- input:

  Input AST node

- predicates:

  List of predicate structures

## Value

An ast_filter node
