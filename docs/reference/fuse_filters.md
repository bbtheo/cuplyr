# Mark filter nodes for fused AND-mask lowering

Mark filter nodes for fused AND-mask lowering

## Usage

``` r
fuse_filters(ast)
```

## Arguments

- ast:

  Root AST node

## Value

AST with fused flags set
