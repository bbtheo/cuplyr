# Format AST as a string for debugging

Format AST as a string for debugging

## Usage

``` r
ast_to_string(node)
```

## Arguments

- node:

  Root AST node

## Value

Character string representation
